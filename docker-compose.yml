services:
  piper-tts:
    build: .
    container_name: piper-tts
    restart: unless-stopped
    environment:
      - PORT=8080
      - PIPER_MODEL=/voices/en_US-ryan-medium.onnx
    volumes:
      - ./voices:/voices:ro
    ports:
      - "8080:8080"
